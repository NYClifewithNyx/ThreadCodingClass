# 2. Class#2 and HM#2_102025.md

LTS - Long Term Service<br>
<br>
Conditional Statements: 1) If and 2) Switch <br>
if - applies to range <br>
switch - applies to certain/specific conditions (ex. +1, 0, -1, 0/1, radio button) <br>
note: for switch, must insert **break;** otherwise next condition will start before selection <br>
<br>
Comparison Operators: >, <, >=, <=, ==, != (for int)<br>
Note: for text(String), use **.eqauls()** <br>
<br>
Logical Operators : &&, ||, !, ==, !=, etc. <br>
<br>
## Homework#2
### 1) 🎖️과제 - 버스 요금 계산기

**요구사항 상세:**

- 프로그램 시작시 보유현금을 사용자로부터 입력받는다.<br>
- 나이에 따라 버스 요금을 다르게 계산한다.<br>
    - 0~7세 → “무료 승차”<br>
    - 8~19세 → “청소년 요금 000원 (요금은 개발자가 임의로 설정한다)”<br>
    - 20~64세 → “성인 요금 000원 (요금은 개발자가 임의로 설정한다)”<br>
    - 65세 이상 → “경로 우대, 무료 승차”<br>
- 버스를 타기에 보유현금이 부족한 경우, 승차불가로 처리한다.<br>
- 버스를 타기에 보유현금이 충분한 경우, 요금을 정산하고 잔액을 출력한다.<br>
- 승차불가 → 안내메세지<br>
    - ex) 잔액이 부족합니다.<br>
- 승차처리 → 안내메세지 + 잔액표시<br>
    - ex) 청소년입니다. 현재남은 잔액 : 000원<br>
    <br>

void main() {<br>
    int age = 0;<br>
    int price = 0;<br>
    int wallet = 0;<br>
<br>
    Scanner sc = new Scanner(System.in);<br>
<br>
    System.out.println("안녕하세요, 닉스 버스를 이용해주셔서 감사합니다");<br>
    System.out.println("고객님의 나이를 입력하여 주십시오");<br>
    age = sc.nextInt();<br>
    sc.nextLine();<br>
<br>
    if (age >= 0 && age <= 7) {<br>
        System.out.println("당신의 나이는 " + age + "세로, 아동입니다. 무료로 승차가 가능합니다");<br>
    }<br>
    else if (age >= 8 && age <= 19) {<br>
        price = 1500;<br>
        System.out.println("당신의 나이는 " + age + "세로, 청소년입니다. 버스 이용 금액은 " + price + "원 입니다");<br>
    }<br>
    else if (age >= 20 && age <= 64) {<br>
        price = 3000;<br>
        System.out.println("당신의 나이는 " + age + "세로, 성인입니다. 버스 이용 금액은 " + price + "원 입니다");<br>
            }<br>
    else if (age > 64 && age <= 110) {<br>
        price = 2000;<br>
        System.out.println("당신의 나이는 " + age + "세로, 경로 우대가 가능합니다. 버스 이용 금액은 " + price + "원 입니다");<br>
    }<br>
    else {<br>
            System.out.println("적용할 수 있는 연령을 벗어났습니다. 다시 시작해주세요");<br>
            System.exit(0);<br>
    }<br>
<br>
    System.out.println("\n원하시는 충전 금액을 선택하여 주십시오");<br>
    System.out.println("1. 5만원   2. 2만원   3. 1만원   \n4. 5천원   5. 기타");<br>
<br>
    int choice = sc.nextInt();<br>
    sc.nextLine();<br>
<br>
    switch (choice) {<br>
        case 1:<br>
            wallet = 50000;<br>
            System.out.println(wallet + "원 충전되었습니다");<br>
            break;<br>
        case 2:<br>
            wallet = 20000;<br>
            System.out.println(wallet + "원 충전되었습니다");<br>
            break;<br>
        case 3:<br>
            wallet = 10000;<br>
            System.out.println(wallet + "원 충전되었습니다");<br>
            break;<br>
        case 4:<br>
            wallet = 5000;<br>
            System.out.println(wallet + "원 충전되었습니다");<br>
            break;<br>
        case 5:<br>
            System.out.println("\n원하시는 금액을 입력하여 주십시오");<br>
            wallet = sc.nextInt();<br>
            sc.nextLine();<br>
            System.out.println(wallet + "원 충전되었습니다");<br>
            break;<br>
        default:<br>
            System.out.println("유효하지 않은 금액입니다");<br>
    }<br>
    <br>
    System.out.println("\n닉스 버스를 이용하시겠습니까? 이용 금액은 " + price + "원 입니다");<br>
    System.out.println("1. 탄다");<br>
    System.out.println("2. 타지 않는다");<br>
    int choice2 = sc.nextInt();<br>
    sc.nextLine();<br>
<br>
<br>
        switch (choice2) {<br>
            case 1:<br>
                if (wallet >= price) {<br>
                    wallet = wallet - price;<br>
                    System.out.println("닉스 버스를 이용해주셔서 감사합니다. 잔액은 " + wallet + "원 입니다");<br>
                }<br>
                else {<br>
                    System.out.println("잔액이 부족합니다. 현재 잔액은 " + wallet + "원 입니다");<br>
                    System.exit(0);<br>
                }<br>
                break;<br>
            case 2:<br>
                System.out.println("다음 기회에 이용해주세요. 감사합니다");<br>
                System.exit(0);<br>
                break;<br>
        }<br>
 }<br>


### 2) 🎖️과제 - 회원가입 및 로그인, 회원정보 조회
**요구사항 명세:**
- 회원가입<br>
    - 프로그램이 시작되면 회원가입 절차가 진행된다<br>
    - 회원가입은, 아이디, 1차-비밀번호, 2차-비밀번호를 입력받고, 추가로 기타 기본 개인정보도 입력받는다. (추가로 입력받는 개인정보는 개발자가 임의로 지정한다)<br>
    - 모든 입력정보가 끝나면, 간략한 개인정보처리약관을 출력 후 의사표시를 입력받는다<br>
    - 개인정보처리 약관 동의 의사에 따라, 상황을 적절히 처리한다.<br>
        - ex) 약관 동의 없이는…<br>
- 로그인<br>
    - 회원가입 절차가 정상적으로 완료되면, 바로 로그인 단계가 실행된다.<br>
    - 아이디와 비밀번호 모두 일치하면 “로그인 성공”<br>
    - 아이디만 다르면 “아이디 불일치”를 표시한다.<br>
    - 비밀번호만 다르면 “비밀번호 불일치”를 표시한다.<br>
    - 로그인이 완료되면 회원정보를 아래와 같이 이쁘게 표시해준다.<br>
<br>
<aside>
⚠️개인정보 보호법상, 로그인 실패가 어떤 정보가 불일치하는 지 알려주는 건 맞지 않으나, 과제 이므로^^<br>
<br>
</aside>
<br>
void main() {<br>
    Scanner sc = new Scanner(System.in);<br>
    String id;<br>
    String pw;<br>
    String pw2;<br>
    String nickname;<br>
    int year = 0;<br>
    int month = 0;<br>
    int day = 0;<br>
    double phone;<br>
    String reason;<br>
<br>
    System.out.println("\n안녕하세요! 닉스 커뮤니티에 오신 것을 환영합니다");<br>
    System.out.println("신규 가입을 위해서 정보를 입력하여 주시기 바랍니다");<br>
<br>
    System.out.println("\n커뮤니티에서 사용할 아이디를 입력해주세요");<br>
    id = sc.nextLine();<br>
    System.out.println("이 아이디로 하시겠습니까?: " + id);<br>
    System.out.println("1. 네   2. 아니오");<br>
    int choice = 0;<br>
    choice = sc.nextInt();<br>
    sc.nextLine();<br>
<br>
    switch (choice) {<br>
        case 1:<br>
            break;<br>
        case 2:<br>
            System.out.println("아이디를 다시 입력해주십시오");<br>
            id = sc.nextLine();<br>
            break;<br>
    }<br>
<br>
    System.out.println("\n비밀번호를 입력하여 주십시오 (추천 : 8자리 이상, 숫자와 특수기호 */!@#$%^ 등을 포함해서");<br>
    pw = sc.nextLine();<br>
    System.out.println("다시 한 번 비밀번호를 입력하여 주십시오");<br>
    pw2 = sc.nextLine();<br>
<br>
    if (pw.equals(pw2)) {<br>
        System.out.println("비밀번호가 일치합니다");<br>
    }<br>
    else {<br>
        System.out.println("비밀번호가 일치하지 않습니다. 다시 입력해주십시오");<br>
        pw2 = sc.nextLine();<br>
        if (!pw.equals(pw2)){<br>
            System.out.println("비밀번호가 다시 일치하지 않습니다. 처음부터 다시 가입을 시도해주세요");<br>
            System.exit(0);<br>
        }<br>
    }<br>
<br>
    System.out.println("\n커뮤니티에서 쓸 닉네임을 정해주세요");<br>
    nickname = sc.nextLine();<br>
<br>
    System.out.println("\n탄생 년도를 입력해주세요 (YYYY 포맷)");<br>
    year = sc.nextInt();<br>
    sc.nextLine();<br>
    if (year <= 1900 || year > 2025) {<br>
        System.out.println("1900년생 이전이나 2025년생 이후는 불가능합니다. 다시 입력해주십시오");<br>
        year = sc.nextInt();<br>
        sc.nextLine();<br>
    }<br>
    System.out.println("\n탄생 월을 입력해주세요");<br>
    month = sc.nextInt();<br>
    sc.nextLine();<br>
    if (month > 12 || month == 0) {<br>
        System.out.println("잘못된 입력입니다. 1월부터 12월 사이의 숫자만 가능합니다. 다시 입력해주십시오");<br>
        month = sc.nextInt();<br>
        sc.nextLine();<br>
    }<br>
<br>
    System.out.println("\n탄생 일을 입력해주세요");<br>
    day = sc.nextInt();<br>
    sc.nextLine();<br>
    if (day == 0 || day > 31) {<br>
        System.out.println("잘못된 입력입니다. 1일부터 31일 사이의 숫자만 가능합니다. 다시 입력해주십시오");<br>
        month = sc.nextInt();<br>
        sc.nextLine();<br>
    }<br>
<br>
    System.out.println("다음과 같이 생년월일이 지정되었습니다: " + year + "/" + month + "/" + day);<br>
<br>
    System.out.println("\n전화번호를 적어주십시오 (숫자만 적어주세요.예: 01012345678)");<br>
    phone = sc.nextDouble();<br>
    sc.nextLine();<br>
    /*<br>
    String strPhone = Double.toString(phone);<br>
    int length = strPhone.length();<br>
    if(length > 8) {<br>
        System.out.println("올바르지 않은 전화번호입니다. 다시 입력해주세요");<br>
        phone = sc.nextInt();<br>
        sc.nextLine();<br>
    }<br>
    시도해봤으나 제대로 되지 않아서 패스...<br>
    */<br>
<br>
    System.out.println("\n가입 목적을 적어주세요.");<br>
    reason = sc.nextLine();<br>
<br>
    System.out.println("\n감사합니다. 아래 약관을 잘 읽고 동의를 해주세요");<br>
    System.out.println("\n(대충 약관이 매우 길게 있음)");<br>
    System.out.println("\n\n약관에 동의하신다면 1을, 동의하지 않으시면 2를 누르고 엔터를 쳐주세요");<br>
    System.out.println("1. 동의합니다    2. 동의하지 않습니다");<br>
<br>
    int choice2 = 0;<br>
    choice2 = sc.nextInt();<br>
    sc.nextLine();<br>
<br>
    switch (choice2) {<br>
        case 1:<br>
            System.out.println("약관에 동의해주셔서 감사합니다. 커뮤니티 가입이 완료되었습니다");<br>
            break;<br>
        case 2:<br>
            System.out.println("약관에 동의하지 않아 커뮤니티 가입이 불발되었습니다.");<br>
            System.exit(0);<br>
            break;//필요 없는 것 같으나 일단 넣어봄<br>
    }<br>
<br>
    //community login<br>
    String userId;<br>
    String userPw;<br>
<br>
    System.out.println("\n\n안녕하세요! 닉스 커뮤니티에 오신 것을 환영합니다");<br>
    System.out.println("아이디를 입력해주세요");<br>
    userId = sc.nextLine();<br>
    System.out.println("비밀번호를 입력해주세요");<br>
    userPw = sc.nextLine();<br>
<br>
    if (userId.equals(id) && userPw.equals(pw)) {<br>
        System.out.println("\n\n============================================");<br>
        System.out.println("                 USER PROFILE               ");<br>
        System.out.println("============================================");<br>
        System.out.println("\n아이디   : " + id);<br>
        System.out.println("닉네임   : " + nickname);<br>
        System.out.println("생년월일 : " + year + "/" + month + "/" + day);<br>
        System.out.println("가입 이유: " + reason);<br>
        System.out.println("\n===========================================");<br>
        System.out.println("(COPYRIGHT) 1980s DOS STYLE PROFILE DISPLAY");<br>
        System.out.println("===========================================");<br>
    }<br>
    else if (!userId.equals(id)) {<br>
        System.out.println("아이디가 맞지 않습니다. 다시 시도해주세요");<br>
        //System.exit(0); < 있으나 없으나 나가게 됨<br>
    }<br>
    else if (!userPw.equals(pw)) {<br>
        System.out.println("패스워드가 일치하지 않습니다. 다시 시도해주세요");<br>
        System.exit(0);<br>
    }<br>
    else {<br>
        System.out.println("잘못된 접근입니다.");<br>
        System.exit(1);<br>
    }<br>
<br>
<br>
}

