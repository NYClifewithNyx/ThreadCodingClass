# 자바 기초 - 반복문과 자료형 (Loop & Array)
<br>
Prev. HM#2 Code Review: <br>
Computer is not capable for calculating decimals. Therefore, for decimal comparison, use a = 0.01, b = 0.01, a - b < 0.0001, etc.<br>
<br>
주석 = comment // /**/ - use it only if code itself cannot explain enough & reason why for warning purpose. <br>
Good to leave some comments on the code you don't understand it well <br><br>

**Default** - only when the user chooses sth outside in the range of switch <br>
It is like an "else" in "if" conditional sentense. <br>
<br>

**int** choice2; this was the problem caused error. If used choice = sc.nextIn(); it would work.<br>
no need to "assign" int again.<br>
  
**Reference Youtube:** Team Nova < Mr. Sangyeop Lee T's final boss <br>
<br>
else { <br>
  if { <br>
  } < may work outsdie the else part.<br>
}<br>
<br>
find auto allign function when saved for IntelliJ ver. <br>
<br>
<br>
# Class#3 - Loop & Array
**for** - used when the loop has a certain number of repeat terms. ex. for 5 loops (i = 1; i <= 5; i++), etc.<br>
**while** - used when the loop can be infinite at certain bootlean condition (true || false) <br>
*use "break;" to escape from while loop, or use FLAG (variable can be named differently) to escape.<br>
Ex. bootlean flag = True;<br>
while (flag) {<br>
      **use condiiton that makes flag = False, then the loop will stop<br>
}<br>
<br>
## Array
Array - SAVING same type data (like a folder) <br>
JAVA - can contain the SAME TYPE while python does not care much<br>
Ex. Int[] numbers = {10, 20, 30, 40, 50};<br>
[] < array <br>
numbers.length is 5;<br>
while counting (index) starts from **[0]** <br>
<br>
ex. numbers[0] = 10;<br>
<br>
<br>
# Homework_3-1
## 과제 - 구구단 출력기
### 요구사항 명세 :
사용자가 구구단의 단과 범위를 입력하면 해당 단의 결과를 출력한다.<br>
출력이 끝난 뒤 “계속할까요?”라는 질문에 따라 프로그램의 흐름이 달라진다.<br>
- **계속한다(y)** → 다시 단과 범위를 입력받아 구구단을 반복 출력한다.<br>
- **그만둔다(n)** → 지금까지 배열에 저장해 둔, 계산된 모든 결과값의 총합을 출력하고 프로그램을 종료한다.<br>
    - 배열은 일정한 크기를 가지며(하드코딩 및 개발자가 프로그램 시작시 지정), 저장 공간이 가득 차면 **가장 오래된 값부터 순서대로 덮어쓴다.** (마치 카세트테이프가 앞에서부터 다시 녹음되듯이).<br>
<br>
⚠️배열에 총합을 저장하거나 덮어쓰는 부분이 너무 헷갈리면 건너뛰고, 구구단 출력까지만 구현해도 좋음<br>
<br>
<br>
# Homework_3-2
## 과제 - 도형그리기
### 요구사항 명세 :
사용자가 도형 그리기를 중단하지 않는 한, 프로그램은 반복적으로 실행된다.<br>
사용자가 입력한 숫자에 따라 도형의 크기가 커지거나 작아지며,<br>
입력된 형태에 따라 세모, 역세모, 네모, 원, 별 등의 도형을 그린다.<br>
도형 하나를 출력한 후에는<br>
“계속할까요?”라는 질문을 통해 다음 실행 여부를 결정한다.<br><br>
- **예(y)** → 다시 숫자와 도형을 입력받아 반복 실행<br>
- **아니오(n)** → 프로그램 종료<br><br>
이 과정에서 반복문으로 전체 실행 흐름을 제어하고,<br>
flag 변수를 통해 반복 여부를 제어한다.<br>
모든 도형은 사용자가 지정한 크기에 맞춰 출력예시된다.<br><br>
